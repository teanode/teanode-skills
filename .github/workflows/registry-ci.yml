name: Registry CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate index structure
        run: |
          set -euo pipefail
          jq -e '
            .skills and
            (.skills | type == "array") and
            all(.skills[]; .name and .description and .version and .url and .sha256 and .signature)
          ' index.json >/dev/null

      - name: Validate files, URLs, and sha256
        run: |
          set -euo pipefail
          jq -c '.skills[]' index.json | while read -r skill; do
            name="$(jq -r '.name' <<<"$skill")"
            version="$(jq -r '.version' <<<"$skill")"
            url="$(jq -r '.url' <<<"$skill")"
            sha="$(jq -r '.sha256' <<<"$skill" | tr '[:upper:]' '[:lower:]')"

            path="skills/${name}/${version}/skill.md"
            expected_url="https://raw.githubusercontent.com/teanode/teanode-skills/main/${path}"

            if [[ ! -f "$path" ]]; then
              echo "Missing skill file: $path"
              exit 1
            fi

            if [[ "$url" != "$expected_url" ]]; then
              echo "URL mismatch for ${name}@${version}"
              echo " expected: $expected_url"
              echo " actual:   $url"
              exit 1
            fi

            actual_sha="$(sha256sum "$path" | awk '{print $1}')"
            if [[ "$sha" != "$actual_sha" ]]; then
              echo "sha256 mismatch for ${name}@${version}"
              echo " expected(index): $sha"
              echo " actual(file):    $actual_sha"
              exit 1
            fi
          done

      - name: Verify signatures (warning on PRs)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: scripts/verify-index.sh --public-key-file keys/teanode-skills-ed25519-public.pem --index index.json

      - name: Verify signatures (required on main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: scripts/verify-index.sh --public-key-file keys/teanode-skills-ed25519-public.pem --index index.json
